# Hronos_torfa — перенастройка: ежечасный прогон, пояснительная записка, алерты

**Дата:** 2026-06-28
**Статус:** утверждён дизайн, готов к плану реализации

## Цель

Доработать пайплайн хронометража транспортировки торфов и **перенести его с Mac
(launchd) на сервер `geol-deploy` (37.140.195.93)** — тот же прод-сервер, что у Geol:

1. Формировать отчёт **ежечасно в окне 6:00–22:00 по Москве** (17 прогонов/день).
2. Добавить **пояснительную записку** — связный текст по итогам дня, генерируемый
   моделью через SSH-мост к Claude Code (тот же приём, что в проекте Geol), с
   детерминированным запасным вариантом «по правилам».
3. Добавить **алерты в Telegram** — о сбоях пайплайна и об аномалиях KPI.
4. Временно слать письма **только на `alexeimvc@gmail.com`** (остальных получателей
   сохранить закомментированными).
5. **Развернуть весь проект (пайплайн + бот) на сервере**: Python-venv, cron для
   пайплайна, systemd для бота. launchd на Mac отключаем.

Принцип: ни одна новая часть не должна ломать существующий отчёт. Мост и алерты —
best-effort; сбой моста или Telegram не прерывает сборку XLSX/PDF.

## Инвентаризация сервера geol-deploy (проверено)

- Доступ: `ssh geol-deploy` (HostName 37.140.195.93, User deploy, key
  `~/.ssh/geol_deploy`). Внешний IP сервера — 37.140.195.93.
- OS: Ubuntu 20.04.6, x86_64. **Python 3.8.10** системный, `venv` и `pip` есть,
  **gcc отсутствует** → зависимости ставить только из готовых wheels.
- `cron` активен, `systemd` 245, **sudo без пароля** (`NOPASSWD`), `Linger=no`.
- Мост к Claude **работает с сервера**: `~/.ssh/eu_claude` присутствует
  (`/home/deploy/.ssh/eu_claude`), `claude --version` через SSH → 2.1.153.
- Сеть: IMAP `imap.yandex.ru:993` — **OK**; Telegram `api.telegram.org:443` —
  **OK**; PyPI — OK. **Исходящий SMTP (465 и 587) — заблокирован** (датацентр).
  EU-сервер моста (185.200.179.31) Yandex SMTP **видит** (220 OK).
- Ресурсы: RAM ~3.9 ГБ (занято ~0.5), диск / — 67 ГБ свободно. TZ сервера —
  проверить на деплое (cron планируем в `Europe/Moscow`, см. ниже).

## Внешняя зависимость: исходящий SMTP

Отправка писем (`send_email.py`, `SMTP_SSL` на `smtp.yandex.ru:465`) **с сервера
не работает**, пока хостинг не откроет исходящий 465/587. **Решение (выбор
пользователя):** запросить у хостинга (reg.ru) открытие исходящего SMTP. Код
отправки при этом **не меняется** — заработает автоматически после разблокировки.

- Действие вне кода: обращение в поддержку хостинга (выполняет пользователь; можем
  подготовить текст запроса).
- Проверка после открытия порта: `.venv/bin/python send_email.py --check` →
  `[OK] SMTP-логин принят`.
- **Интерим (пока порт закрыт):** отчёт всё равно формируется ежечасно; доставка
  результата идёт **через Telegram** — уведомление бота с текстом записки и команды
  `/pdf` `/xlsx`. Алерты и записка от SMTP не зависят. Рассылка email включается
  без правок кода, как только порт откроют.

## Контекст (как есть сейчас)

- Оркестратор [run_daily.py](../../../run_daily.py): `fetch → consolidate → build_pdf →
  notify_bot`. Письма уходят только после подтверждения в боте.
- Расписание [scripts/com.alexei.hronos_torfa.plist](../../../scripts/com.alexei.hronos_torfa.plist):
  `StartCalendarInterval`, каждый час xx:10 с 9 до 19.
- Аналитика [build_pdf.py](../../../build_pdf.py): считает агрегаты (по подразделениям,
  датам, часам, водителям/ABC, паркам, простоям) и рисует PDF. Все нужные для записки
  и алертов числа уже вычисляются внутри `main()`.
- Рассылка [send_email.py](../../../send_email.py): уже принимает `--note` (текст в тело
  письма) и пропускает строки `recipients.txt`, начинающиеся с `#`.
- Бот [bot.py](../../../bot.py): long-polling, команды управления, админ-чат в
  `state/admin_chat_id.txt`.
- Мост-образец Geol `webservice/app/llm.py`: `available()` + `ask(prompt)` →
  `ssh -i ~/.ssh/eu_claude root@185.200.179.31 "claude -p"`, промпт в stdin, без
  API-ключа. Ключ `~/.ssh/eu_claude` присутствует локально.

## Поток выполнения run_daily

`fetch → consolidate → build_pdf → explain → (алерты об аномалиях) → notify_bot`.
Алерты о сбоях шага отправляются в точке сбоя (до раннего выхода). Шаг `explain`
идёт после `build_pdf`, т.к. читает `state/last_metrics.json`.

## Часть 1. Расписание → ежечасно 6:00–22:00 (на сервере, cron)

Планировщик переезжает с launchd (Mac) на **cron сервера geol-deploy**.

- Crontab пользователя `deploy`: `CRON_TZ=Europe/Moscow` + строка
  `10 6-22 * * * cd /home/deploy/hronos_torfa && .venv/bin/python run_daily.py >> logs/cron.log 2>&1`
  (17 прогонов/день, xx:10, 6…22 по Москве). `CRON_TZ` нужен, т.к. локальное
  время сервера может быть UTC.
- launchd на Mac **отключается** (см. Часть 5 — decommission). Файлы plist
  остаются в репозитории как историческая справка, но больше не используются.
- README обновить: окно 6–22, запуск на сервере через cron.

## Часть 2. Пояснительная записка через мост

### 2.1 Модуль `llm.py` (новый)

Копия моста из Geol, минимально необходимая поверхность:

- `available() -> bool` — есть ли SSH-ключ.
- `ask(prompt: str, timeout: int | None = None) -> str` — `ssh … "claude -p"`,
  промпт в **stdin** (не аргументом — иначе упираемся в ARG_MAX). Бросает
  `RuntimeError` при недоступности ключа/ненулевом коде.

Конфиг через env с дефолтами (значения уже известны, в код зашиты как умолчания):
`EU_CLAUDE_SERVER=185.200.179.31`, `EU_CLAUDE_USER=root`,
`EU_CLAUDE_KEY=~/.ssh/eu_claude`, `EU_CLAUDE_TIMEOUT=300`. Добавить эти ключи в
`.env.example` с комментарием. Зависимости — только стандартная библиотека.

### 2.2 Метрики из `build_pdf.py`

`build_pdf.py` в конце `main()` дополнительно сохраняет компактный
`state/last_metrics.json` (переиспользуя уже посчитанные DataFrame'ы). Состав:

- `report_date`, `period` (min/max дат);
- итоги: `total_volume`, `total_trips`, `m3_per_trip`, `n_drivers`, `n_trucks`,
  `n_units`;
- `by_unit`: список {подразделение, рейсы, объём, м³/рейс, доля_%};
- `by_date`: список {дата, объём, рейсы} (для динамики день-к-дню);
- `truck_util`: средняя загрузка кузова, % (общая и по маркам — мин/среднее);
- `idle`: суммарные часы простоя за отчётную дату (по подразделениям);
- `abc`: число водителей A/B/C.

Это чистый дамп словаря — на отрисовку PDF не влияет, выполняется без сети.

### 2.3 Модуль `explain.py` (новый шаг)

1. Читает `state/last_metrics.json`.
2. Формирует короткий «бриф данных» (плоский текст с цифрами) и системную инструкцию:
   деловой стиль, 3–6 абзацев — что произошло за день, ключевые цифры и динамика
   к прошлому дню, узкие места (низкая загрузка/простои), краткие рекомендации;
   **ничего не выдумывать сверх переданных чисел**.
3. Если `llm.available()` — `llm.ask(brief)`, иначе (или при `RuntimeError`) —
   `_rule_based_note(metrics)`: та же структура, собранная шаблоном из чисел.
4. Сохраняет `output/Пояснительная_записка_<дата>.md` и `state/last_note.txt`.
   Возвращает текст записки (для писем/бота).

Запуск: `python3 explain.py` (самодостаточно, читает state). Идемпотентен.

### 2.4 Размещение записки

- Тело письма: `send_email.py --note "<текст записки>"` (механизм уже есть).
- Подтверждающее сообщение бота: добавить первые ~600 символов записки в текст
  `notify_bot(...)`.
- Файл `output/Пояснительная_записка_<дата>.md` — дополнительное вложение письма
  (расширить `send_email.py`, чтобы помимо xlsx/pdf прикладывать записку, если есть).

**Решение:** записка — отдельный документ, **не** страница внутри PDF. Причины:
PDF остаётся детерминированным и быстрым, сбой/задержка моста не влияет на PDF,
и «пояснительная записка» по смыслу — самостоятельный документ. (Вариант «страницей
в PDF» отклонён как более связанный и медленный.)

## Часть 3. Алерты в Telegram

Канал — админ-чат бота, через `tg_request` (уже есть в `run_daily.py`). Вынести в
хелпер `alert(env, text)`.

### 3.1 Сбои

- `consolidate.py` или `build_pdf.py` завершились с ненулевым кодом → алерт с шагом
  и кодом возврата.
- `fetch` не нашёл свежих вложений / дата отчёта (`last_fetch.meta`) не сегодняшняя →
  алерт «нет свежих данных за <дата>».

Сейчас эти ситуации только логируются — добавляем отправку в Telegram (логи остаются).

### 3.2 Аномалии

Считаются в `explain.py` из `state/last_metrics.json` и сохраняются в
`state/last_alerts.json` — список объектов `{type, text}`. `run_daily.py` после шага
`explain` читает этот файл и прогоняет каждый алерт через `alert()` (с анти-спамом).
Пороги — константы в одном месте (с возможностью переопределить через `.env`):

- `MIN_BODY_UTIL = 95` — средняя загрузка кузова, % (ниже → алерт);
- `MAX_IDLE_H = 1.0` — простои, ч/смену (выше → алерт);
- `MAX_VOLUME_DROP = 30` — падение объёма к прошлому дню, % (больше → алерт).

### 3.3 Анти-спам

При 17 прогонах/день один и тот же алерт не должен повторяться. Файл
`state/alerts_sent.json` — множество ключей `"<дата>|<тип_алерта>"`. Перед отправкой
проверяем ключ; отправляем только новый; после отправки сохраняем. Сброс — естественно
при смене даты (старые ключи можно чистить, оставляя только сегодняшние).

## Часть 4. Получатели

- В [recipients.txt](../../../recipients.txt) закомментировать (`#`) 4 адреса
  `pskgold.ru`, оставить активным только `alexeimvc@gmail.com`.
- Логику менять не нужно — `load_recipients()` уже игнорирует `#`-строки.

## Часть 5. Развёртывание на сервере geol-deploy

Цель — весь проект работает на сервере, Mac отключаем. Размещение:
`/home/deploy/hronos_torfa`.

### 5.1 Runtime и зависимости

- venv: `python3 -m venv /home/deploy/hronos_torfa/.venv` (Python 3.8).
- Зависимости только из wheels (gcc нет): `pip install --only-binary=:all:`
  с пинами под 3.8 — `pandas==2.0.3`, `numpy==1.24.4`, `matplotlib==3.7.5`,
  `openpyxl==3.1.x`. (Точные версии проверить на наличие cp38-wheel в плане.)
- `matplotlib` использует backend `Agg` (уже задан в `build_pdf.py`) и встроенный
  шрифт DejaVu Sans — системные шрифты не нужны.

### 5.2 Перенос кода и секретов

- Код: `rsync` из `/Users/alexei/Claude_v1` → `geol-deploy:/home/deploy/hronos_torfa`,
  исключая `.git __pycache__ *.pyc .venv output logs input docs`.
- `.env` (gitignored, содержит IMAP/SMTP-креды и `BOT_TOKEN`) — переносится
  **отдельно по rsync/scp**, в git не попадает. `EU_CLAUDE_*` можно не задавать:
  дефолты в `llm.py` указывают на `~/.ssh/eu_claude` (= `/home/deploy/.ssh/eu_claude`).
- `recipients.txt` (с закомментированными адресами) — переносится.
- Создать каталоги `logs/ state/ output/ input/` на сервере.
- Скрипт деплоя `scripts/deploy_server.sh` (rsync + venv + pip + рестарт бота) —
  по образцу процедуры Geol (`project-geol-deploy`), с retry-циклом на
  `banner exchange timeout`.

### 5.3 Бот как systemd-сервис

- Системный unit `hronos-bot.service` (ставится через `sudo`, т.к. `Linger=no`):
  `ExecStart=/home/deploy/hronos_torfa/.venv/bin/python bot.py`,
  `WorkingDirectory=/home/deploy/hronos_torfa`, `User=deploy`, `Restart=always`,
  `RestartSec=5`. `enable` + `start`. Файл — `scripts/hronos-bot.service` в репо.
- Бот при старте регистрирует админ-чат (`/start`) — выполнить один раз после
  запуска на сервере.

### 5.4 Decommission Mac

- Выгрузить и снять автозапуск обеих launchd-задач:
  `launchctl bootout gui/$(id -u)/com.alexei.hronos_torfa` и `…_bot` (или
  `launchctl unload` + убрать из `LaunchAgents`), чтобы не было двойных прогонов и
  двух ботов на одном токене (иначе конфликт long-polling `getUpdates`).
- `scripts/uninstall_launchd.sh` уже есть — использовать его.

### 5.5 Проверки после деплоя (smoke)

- `crontab -l` показывает строку 6–22; `systemctl status hronos-bot` — active.
- Ручной прогон `.venv/bin/python run_daily.py` → собираются xlsx/pdf,
  `Пояснительная_записка_*.md`, приходит уведомление бота с текстом записки.
- Мост: при наличии данных записка — «живой» текст; при недоступности моста —
  правиловый фолбэк, отчёт собран.
- Тест алерта: искусственный сбой шага и заведомо аномальные данные → ровно один
  алерт на уникальный случай за день.
- Email: `send_email.py --check` (ожидаемо упадёт, пока хостинг не откроет SMTP —
  это известная внешняя зависимость; повторить после открытия порта).

## Опционально (по подтверждению)

- Команда бота `/note` — прислать `state/last_note.txt` / последний
  `Пояснительная_записка_*.md`. (Решено: **пока не делаем**.)

## Затрагиваемые файлы

| Файл | Изменение |
|---|---|
| `llm.py` | новый — SSH-мост (копия паттерна Geol) |
| `explain.py` | новый — генерация записки + список аномалий |
| `build_pdf.py` | дамп `state/last_metrics.json` |
| `run_daily.py` | вызов `explain.py`; хелпер `alert()`; алерты о сбоях/аномалиях; записка в `notify_bot` и в `--note` |
| `send_email.py` | прикладывать файл записки как вложение (код SMTP без изменений) |
| `recipients.txt` | закомментировать 4 адреса |
| `.env.example` | ключи `EU_CLAUDE_*` и пороги аномалий |
| `scripts/deploy_server.sh` | новый — rsync + venv + pip + рестарт бота на geol-deploy |
| `scripts/hronos-bot.service` | новый — systemd-unit бота |
| `scripts/com.alexei.hronos_torfa*.plist` | выводятся из эксплуатации (Mac decommission) |
| `README.md` | окно 6–22, запуск на сервере (cron+systemd), записка/алерты, зависимость по SMTP |

Расписание задаётся не файлом в репо, а crontab пользователя `deploy` на сервере
(`CRON_TZ=Europe/Moscow`, строка 6–22). Команда `/note` в `bot.py` — отложена.

## Критерии готовности

- На сервере: `cron` срабатывает в 6:10…22:10 (МСК), `systemctl status hronos-bot`
  active; в `logs/` видны прогоны. На Mac launchd-задачи сняты (нет двойных прогонов
  и второго бота на том же токене).
- После прогона существует `output/Пояснительная_записка_<дата>.md`, её текст — в
  сообщении бота (и в теле письма, когда SMTP откроют); при выключенном мосте записка
  всё равно формируется (правиловый вариант), PDF/XLSX собираются как прежде.
- При искусственном сбое шага и при заведомо аномальных данных приходит ровно один
  алерт в Telegram на каждый уникальный случай за день.
- Получатель email — только `alexeimvc@gmail.com`. Сама отправка заработает после
  открытия исходящего SMTP хостингом (внешняя зависимость); до этого результат
  доставляется через Telegram.

## Вне рамок (YAGNI)

- Перенос аналитики на другой стек/БД.
- Веб-интерфейс, история записок, графики трендов за длинный период.
- Авто-отправка писем без подтверждения (страховка из проектных правил сохраняется).
